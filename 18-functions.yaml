- name: functions
  hosts: local
  connection: local
  vars:
    technologies: Devops, Linux, Shell, Ansible ## string

  #### dictopnary = map
    tags:
      Application: Payment 
      Environment: dev
    ## List
    listTags:  
      - key: Application
        value: Payment
      - key: Environment
        value: dev
  tasks:
  - name: default variable
    ansible.builtin.debug:
      msg: "Hello, {{ name | default('Naresh') }}"

 ### String to list

  - name: convert string to list
    ansible.builtin.debug:
      msg: "Technologies are: {{ technologies | split(',') }}"

  - name: convert map to list
    ansible.builtin.debug:
      msg: "tags are: {{ tags | dict2items }}"

  - name: convert list into map
    ansible.builtin.debug:
      msg: "tags map: {{ listTags | items2dict }}" 

  - name: read yaml and take it into variable
    ansible.builtin.set_fact:
      student_details.yaml: "{{ lookup('file', 'student_details.yaml') | from_yaml }}"  

  - name: convert yaml into json
    ansible.builtin.debug:
      msg: " json: {{ student_details.yaml | to_json }}"